<p class="lead">Welcome to your homepage</p>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Date of birth</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @peoplelist.each do |person| %>
    <tr>
      <td><%= person.first_name %></td>
      <td><%= person.last_name %></td>
      <td><%= person.date_of_birth %></td>
      <td><%= link_to 'Show', person %></td>
      <td><%= link_to 'Edit', edit_person_path(person) %></td>
      <td><%= link_to 'Destroy', person, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Number of employees</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orglist.each do |orginization| %>
    <tr>
      <td><%= orginization.name %></td>
      <td><%= orginization.number_of_employees %></td>
      <td><%= link_to 'Show', orginization %></td>
      <td><%= link_to 'Edit', edit_orginization_path(orginization) %></td>
      <td><%= link_to 'Destroy', orginization, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Date of birth</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @alienlist.each do |alien| %>
    <tr>
      <td><%= alien.first_name %></td>
      <td><%= alien.last_name %></td>
      <td><%= alien.date_of_birth %></td>
      <td><%= link_to 'Show', alien %></td>
      <td><%= link_to 'Edit', edit_alien_path(alien) %></td>
      <td><%= link_to 'Destroy', alien, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>
<!-- <%  @peoplelist.each do |person| %>
  <div><%= person.first_name %></div>
  <div><%= person.last_name %></div>
  <div><%= person.date_of_birth %></div>

  <% end %> -->




<!-- <% @orglist.each do |org| %>
  <div><%= org.name %></div>
  <div><%= org.number_of_employees %></div>

  <% end %> -->
